{
  "name": "vx-chrome",
  "version": "0.6.0",
  "devDependencies": {
    "create-vite": "^3.2.1",
    "@types/chrome": "^0.0.203"
  },
  "scripts": {
    "preinitproject": "npm install && npm update",
    "initproject": "npm install --prefix frontend && npm update --prefix frontend",
    "postinitproject": "cd frontend && npm run build && npm run preview",
    "pack": "npm pack --pack-destination tmp"
  },
  "files": [
    "src",
    "assets",
    "background.js",
    "icon.png",
    "icon-128.png",
    "manifest.json",
    "options.html",
    "options.js",
    "popup.html",
    "popup.js"
  ],
  "private": true
}
